{
  "mcpServers": {
    "tidal-dashboard": {
      "command": "node",
      "args": ["dashboard/packages/mcp/dist/index.js"],
      "env": {
        "TIDAL_API_URL": "${TIDAL_API_URL}",
        "TIDAL_API_TOKEN": "${TIDAL_API_TOKEN}"
      }
    }
  }
}
