// Placeholder — real tests are in hooks/useExperimentActions.test.ts
import { describe, it } from "vitest";
describe("useExperimentActions __tests__ placeholder", () => {
  it("is a placeholder — see hooks/useExperimentActions.test.ts", () => {});
});
